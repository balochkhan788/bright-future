export default function WithdrawalHistory() {
  const withdrawals = [
    {
      id: "WD-001",
      amount: "Rs. 5,000",
      date: "11 Sep 2026",
      status: "Pending",
    },
    {
      id: "WD-002",
      amount: "Rs. 3,000",
      date: "08 Sep 2026",
      status: "Approved",
    },
  ];

  return (
    <main className="min-h-screen bg-slate-950 p-6 text-white">
      <div className="mx-auto max-w-5xl">

        <div className="flex items-center justify-between">
          <div>
            <h1 className="text-3xl font-bold">
              Withdrawal <span className="text-cyan-400">History</span>
            </h1>

            <p className="mt-1 text-sm text-slate-400">
              View your withdrawal requests
            </p>
          </div>

          <a
            href="/dashboard"
            className="rounded-lg border border-white/20 px-4 py-2 text-sm"
          >
            Dashboard
          </a>
        </div>

        <div className="mt-8 overflow-hidden rounded-2xl border border-white/10 bg-white/5">

          <div className="grid grid-cols-4 border-b border-white/10 p-4 text-sm font-bold text-slate-400">
            <span>Request ID</span>
            <span>Amount</span>
            <span>Date</span>
            <span>Status</span>
          </div>

          {withdrawals.map((item) => (
            <div
              key={item.id}
              className="grid grid-cols-4 border-b border-white/10 p-4"
            >
              <span>{item.id}</span>

              <span className="font-bold">
                {item.amount}
              </span>

              <span className="text-slate-400">
                {item.date}
              </span>

              <span
                className={
                  item.status === "Approved"
                    ? "font-bold text-green-400"
                    : "font-bold text-yellow-400"
                }
              >
                {item.status}
              </span>
            </div>
          ))}

        </div>

        <div className="mt-6 rounded-xl border border-cyan-400/20 bg-cyan-400/5 p-5 text-sm text-slate-300">
          Withdrawal requests will appear here after submission.
        </div>

      </div>
    </main>
  );
}